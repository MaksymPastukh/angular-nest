# Комментарии продуктов - NgRx Signal Store

## 📁 Структура файлов

```
frontend/src/app/features/products/detail/
├── components/
│   ├── comments/
│   │   ├── comments.ts         # Компонент списка комментариев
│   │   ├── comments.html       # Шаблон
│   │   └── comments.scss       # Стили
│   └── comment-item/
│       ├── comment-item.ts     # Компонент отдельного комментария
│       ├── comment-item.html   # Шаблон
│       └── comment-item.scss   # Стили
├── store/
│   └── comment.store.ts        # NgRx Signal Store
├── services/
│   └── comment.service.ts      # HTTP сервис для API
└── types/
    └── comment.interface.ts    # Типы и интерфейсы
```

## 🚀 Использование

### 1. В компоненте страницы продукта

```typescript
import { CommentsComponent } from './components/comments/comments'

@Component({
  selector: 'app-product-detail',
  imports: [CommentsComponent],
  template: ` <app-comments [productId]="productId()" /> `,
})
export class ProductDetailComponent {
  productId = signal('your-product-id')
}
```

### 2. Store автоматически:

- ✅ Загружает комментарии при изменении productId
- ✅ Обрабатывает создание комментариев
- ✅ Удаляет комментарии
- ✅ Вычисляет средний рейтинг
- ✅ Сортирует по дате (новые сверху)

## 📊 Доступные методы Store

### Загрузка комментариев

```typescript
commentStore.loadComments(productId)
```

### Создание комментария

```typescript
commentStore.addComment({
  productId: 'product-id',
  comment: {
    text: 'Отличный товар!',
    rating: 5,
  },
})
```

### Удаление комментария

```typescript
commentStore.deleteComment({
  productId: 'product-id',
  commentId: 'comment-id',
})
```

## 📦 Computed значения

- `hasComments()` - есть ли комментарии
- `commentsCount()` - количество комментариев
- `averageRating()` - средний рейтинг
- `sortedComments()` - отсортированные комментарии

## 🎯 Состояние Store

- `comments` - массив комментариев
- `isLoading` - загрузка данных
- `isSubmitting` - отправка комментария
- `error` - текст ошибки
- `total` - общее количество

## 🔌 Backend API

### GET /products/:id

Возвращает продукт с комментариями

### POST /products/:id/comments

Создает новый комментарий (требует авторизации)

**Body:**

```json
{
  "text": "Текст комментария",
  "rating": 5
}
```

### DELETE /products/:id/comments/:commentId

Удаляет комментарий (требует авторизации)

## 🎨 Особенности UI

- ⭐ Рейтинг звездами (1-5)
- 📊 Отображение среднего рейтинга
- 🔄 Индикаторы загрузки
- ❌ Обработка ошибок
- 🌙 Dark mode support
- 📱 Responsive design

## 🔧 Технологии

- **NgRx Signals** - управление состоянием
- **RxJS** - реактивные операции
- **Angular Signals** - реактивность
- **TailwindCSS** - стили
