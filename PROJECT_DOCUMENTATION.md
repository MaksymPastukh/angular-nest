# Документация проекта Marketplace

## 📋 Содержание
- [Общее описание](#общее-описание)
- [Архитектура проекта](#архитектура-проекта)
- [Backend](#backend)
- [Frontend](#frontend)
- [Технологический стек](#технологический-стек)
- [Структура проекта](#структура-проекта)
- [Установка и запуск](#установка-и-запуск)

---

## 🎯 Общее описание

**Marketplace** - это полнофункциональное веб-приложение интернет-магазина, построенное на современном технологическом стеке. Проект разделен на два независимых приложения:
- **Backend** - RESTful API на NestJS
- **Frontend** - SPA приложение на Angular 21

---

## 🏗 Архитектура проекта

Проект использует **монорепозиторий** с четким разделением на frontend и backend части. Обе части взаимодействуют через REST API.

```
┌─────────────────────────────────────────────────────┐
│                   MARKETPLACE                       │
├─────────────────────┬───────────────────────────────┤
│      FRONTEND       │          BACKEND              │
│    (Angular 21)     │        (NestJS 11)            │
│                     │                               │
│  ┌──────────────┐   │   ┌──────────────────────┐   │
│  │   Views      │   │   │   Controllers        │   │
│  │              │   │   │   (REST API)         │   │
│  ├──────────────┤   │   ├──────────────────────┤   │
│  │   Features   │◄──┼──►│   Services           │   │
│  │              │   │   │   (Business Logic)   │   │
│  ├──────────────┤   │   ├──────────────────────┤   │
│  │   Core       │   │   │   Schemas/Models     │   │
│  │   (Auth)     │   │   │   (MongoDB)          │   │
│  ├──────────────┤   │   ├──────────────────────┤   │
│  │   Shared     │   │   │   Guards & Pipes     │   │
│  │              │   │   │   (Validation)       │   │
│  └──────────────┘   │   └──────────────────────┘   │
└─────────────────────┴───────────────────────────────┘
                      │
                      ▼
            ┌──────────────────┐
            │    MongoDB       │
            │    Database      │
            └──────────────────┘
```

---

## 🔧 Backend

### Технологии и инструменты

| Категория | Технология | Назначение |
|-----------|-----------|------------|
| **Framework** | NestJS 11 | Фреймворк для построения масштабируемых серверных приложений |
| **База данных** | MongoDB + Mongoose 9 | NoSQL база данных и ODM |
| **Аутентификация** | Passport.js + JWT | Система аутентификации с токенами |
| **Валидация** | class-validator, class-transformer | Валидация и трансформация данных |
| **Безопасность** | bcrypt | Хеширование паролей |
| **Конфигурация** | @nestjs/config | Управление переменными окружения |
| **Тестирование** | Jest | Unit и E2E тестирование |
| **Статика** | @nestjs/serve-static | Раздача статических файлов (изображения) |

### Архитектура Backend

Backend построен по модульной архитектуре NestJS с использованием принципов **SOLID** и **Clean Architecture**.

#### Основные модули:

```
src/
├── app.module.ts           # Корневой модуль приложения
├── main.ts                 # Точка входа приложения
│
├── auth/                   # 🔐 Модуль аутентификации
│   ├── auth.controller.ts  # Эндпоинты: login, register, refresh
│   ├── auth.service.ts     # Логика аутентификации
│   ├── strategies/         # JWT и Local стратегии Passport
│   ├── guards/             # Защита роутов (JWT, Roles)
│   ├── decorators/         # Декораторы для получения пользователя
│   └── dto/                # Data Transfer Objects для валидации
│
├── users/                  # 👤 Модуль пользователей
│   ├── users.service.ts    # CRUD операции с пользователями
│   └── schemas/            # Mongoose схема User
│
├── products/               # 📦 Модуль товаров
│   ├── products.controller.ts  # CRUD эндпоинты для товаров
│   ├── products.service.ts     # Бизнес-логика товаров
│   ├── schemas/                # Mongoose схема Product
│   └── dto/                    # DTO для создания/обновления товаров
│
├── cart/                   # 🛒 Модуль корзины
│   ├── cart.controller.ts  # Управление корзиной
│   ├── cart.service.ts     # Логика добавления/удаления товаров
│   └── schemas/            # Mongoose схема Cart
│
├── comments/               # 💬 Модуль комментариев
│   ├── comments.controller.ts  # CRUD для комментариев
│   ├── comments.service.ts     # Логика комментариев и рейтингов
│   └── schemas/                # Mongoose схема Comment
│
└── static/                 # 🖼 Модуль статических файлов
    └── static.controller.ts    # Раздача изображений товаров
```

### Основные возможности Backend

#### 1. **Система аутентификации**
- Регистрация пользователей с валидацией данных
- Вход в систему (email + пароль)
- JWT токены (Access + Refresh)
- Автоматическое обновление токенов
- Роли пользователей (User, Admin)
- Защита маршрутов через Guards

#### 2. **Управление товарами**
- CRUD операции для товаров
- Фильтрация и поиск товаров
- Пагинация результатов
- Загрузка изображений товаров
- Категоризация товаров

#### 3. **Корзина покупок**
- Добавление товаров в корзину
- Изменение количества товаров
- Удаление товаров из корзины
- Автоматический расчет общей стоимости
- Привязка корзины к пользователю

#### 4. **Система комментариев и рейтингов**
- Добавление комментариев к товарам
- Оценка товаров (rating)
- Фильтрация комментариев
- Модерация (редактирование/удаление для администраторов)

### API Endpoints

#### Аутентификация (`/auth`)
- `POST /auth/register` - Регистрация нового пользователя
- `POST /auth/login` - Вход в систему
- `POST /auth/refresh` - Обновление токена

#### Товары (`/products`)
- `GET /products` - Получить список товаров (с фильтрами)
- `GET /products/:id` - Получить товар по ID
- `POST /products` - Создать товар (Admin)
- `PUT /products/:id` - Обновить товар (Admin)
- `DELETE /products/:id` - Удалить товар (Admin)

#### Корзина (`/cart`)
- `GET /cart` - Получить корзину текущего пользователя
- `POST /cart` - Добавить товар в корзину
- `PUT /cart/:id` - Обновить количество товара
- `DELETE /cart/:id` - Удалить товар из корзины

#### Комментарии (`/comments`)
- `GET /comments` - Получить комментарии (с фильтрами)
- `POST /comments` - Создать комментарий
- `PUT /comments/:id` - Обновить комментарий
- `DELETE /comments/:id` - Удалить комментарий

---

## 🎨 Frontend

### Технологии и инструменты

| Категория | Технология | Назначение |
|-----------|-----------|------------|
| **Framework** | Angular 21 | Современный SPA framework от Google |
| **Управление состоянием** | @ngrx/signals | Реактивное управление состоянием |
| **UI библиотека** | PrimeNG 21 | Компоненты UI (таблицы, формы, диалоги) |
| **Стили** | Tailwind CSS 4 | Utility-first CSS framework |
| **Иконки** | PrimeIcons | Набор иконок |
| **Анимации** | @angular/animations | Анимации и переходы |
| **Маршрутизация** | @angular/router | SPA роутинг |
| **HTTP клиент** | @angular/common/http | Работа с API |
| **Тестирование** | Vitest | Современный фреймворк для тестирования |
| **Линтинг** | ESLint + Prettier | Контроль качества кода |

### Архитектура Frontend

Frontend использует **модульную архитектуру** с разделением на **Core**, **Features**, **Shared** и **Views**.

```
src/app/
│
├── core/                      # 🔐 Ядро приложения
│   └── auth/                  # Модуль аутентификации
│       ├── components/        # Компоненты (Login, Register, Reset Password)
│       ├── guard/             # Guards (auth.guard, admin.guard)
│       ├── services/          # AuthService, AuthInterceptor
│       ├── store/             # @ngrx/signals хранилище состояния
│       └── types/             # Интерфейсы и типы
│
├── features/                  # 🎯 Функциональные модули
│   ├── personal/              # Личный кабинет
│   │   ├── admin/             # Админ панель
│   │   └── info/              # Информация пользователя
│   └── products/              # Функционал товаров
│       ├── catalog/           # Каталог товаров
│       └── detail/            # Детальная страница товара
│
├── shared/                    # 🔄 Переиспользуемые компоненты
│   ├── components/            # Общие компоненты
│   │   ├── categories-card/   # Карточка категории
│   │   ├── product-card/      # Карточка товара
│   │   ├── rating/            # Компонент рейтинга
│   │   └── table-best-price/  # Таблица лучших цен
│   ├── layout/                # Layout компоненты
│   │   ├── header/            # Шапка сайта
│   │   └── footer/            # Подвал сайта
│   ├── pipes/                 # Pipes (image-url)
│   ├── services/              # Общие сервисы
│   └── types/                 # Общие интерфейсы
│
└── views/                     # 📄 Страницы приложения
    ├── main/                  # Главная страница
    ├── admin-panel/           # Административная панель
    │   └── create-product/    # Создание товара
    ├── orders/                # Заказы
    ├── personal/              # Личный кабинет
    │   ├── favorite/          # Избранное
    │   ├── info/              # Информация
    │   └── orders/            # История заказов
    └── products/              # Страницы товаров
        ├── catalog/           # Каталог
        └── detail/            # Детальная информация
```

### Структура по слоям

#### 1. **Core Layer** (Ядро)
Содержит базовую функциональность, которая используется во всем приложении:
- **Аутентификация**: Login, Register, Password Reset
- **Guards**: Защита маршрутов (auth.guard.ts, admin.guard.ts)
- **Interceptors**: HTTP interceptor для добавления токенов
- **Store**: Глобальное состояние приложения (@ngrx/signals)

#### 2. **Features Layer** (Функционал)
Бизнес-логика приложения, разбитая по доменам:
- **Products**: Каталог, детальная информация о товаре
- **Personal**: Личный кабинет пользователя
- **Admin**: Административные функции

#### 3. **Shared Layer** (Общее)
Переиспользуемые компоненты, сервисы и утилиты:
- **Components**: product-card, categories-card, rating
- **Layout**: header, footer
- **Pipes**: image-url (формирование URL изображений)
- **Services**: categories.service (работа с категориями)
- **Types**: Общие интерфейсы и типы

#### 4. **Views Layer** (Представления)
Контейнерные компоненты страниц:
- Главная страница (main)
- Страницы товаров (catalog, detail)
- Личный кабинет (favorite, orders, info)
- Админ панель (create-product)

### Основные возможности Frontend

#### 1. **Система аутентификации**
- Регистрация пользователей
- Вход в систему
- Восстановление пароля
- Автоматическое обновление токенов через interceptor
- Проверка прав доступа через guards
- Сохранение состояния пользователя в @ngrx/signals store

#### 2. **Каталог товаров**
- Отображение товаров в виде карточек
- Фильтрация по категориям
- Поиск товаров
- Пагинация
- Просмотр детальной информации

#### 3. **Корзина покупок**
- Добавление товаров
- Изменение количества
- Удаление товаров
- Расчет общей стоимости

#### 4. **Личный кабинет**
- Просмотр и редактирование профиля
- История заказов
- Избранные товары

#### 5. **Административная панель**
- Создание/редактирование товаров
- Управление категориями
- Модерация комментариев

### Роутинг

Приложение использует модульную систему маршрутизации:

```typescript
/ (main)                    # Главная страница
/auth
  /login                    # Вход
  /register                 # Регистрация
  /reset-password           # Восстановление пароля
/products
  /catalog                  # Каталог товаров
  /detail/:id              # Детальная страница товара
/personal
  /info                     # Информация профиля
  /orders                   # История заказов
  /favorite                 # Избранное
/admin
  /create-product          # Создание товара
```

### Защита маршрутов

- **AuthGuard**: Проверяет, авторизован ли пользователь
- **AdminGuard**: Проверяет, является ли пользователь администратором
- **AuthForwardGuard**: Перенаправляет авторизованных пользователей

---

## 🛠 Технологический стек

### Backend Stack

```
NestJS 11.0.1                  # Node.js фреймворк
├── MongoDB 9.0.2              # База данных
├── Mongoose 11.0.4            # ODM для MongoDB
├── Passport.js 0.7.0          # Аутентификация
│   ├── passport-jwt 4.0.1     # JWT стратегия
│   └── passport-local 1.0.0   # Local стратегия
├── JWT 11.0.2                 # JSON Web Tokens
├── bcrypt 6.0.0               # Хеширование паролей
├── class-validator 0.14.3     # Валидация DTO
├── class-transformer 0.5.1    # Трансформация данных
└── Jest 30.0.0                # Тестирование
```

### Frontend Stack

```
Angular 21.0.0                 # SPA фреймворк
├── @ngrx/signals 21.0.1      # State management
├── PrimeNG 21.0.2            # UI библиотека
├── Tailwind CSS 4.1.12       # CSS фреймворк
├── RxJS 7.8.0                # Реактивное программирование
└── Vitest 4.0.8              # Unit тестирование
```

---

## 📁 Структура проекта

```
Marketplace/
│
├── backend/                              # Backend приложение (NestJS)
│   ├── src/                              # Исходный код
│   │   ├── auth/                         # Модуль аутентификации
│   │   ├── cart/                         # Модуль корзины
│   │   ├── comments/                     # Модуль комментариев
│   │   ├── products/                     # Модуль товаров
│   │   ├── users/                        # Модуль пользователей
│   │   ├── static/                       # Статические файлы
│   │   ├── app.module.ts                 # Главный модуль
│   │   └── main.ts                       # Точка входа
│   ├── public/images/products/           # Изображения товаров
│   ├── test/                             # E2E тесты
│   ├── .env                              # Переменные окружения
│   ├── package.json                      # Зависимости
│   ├── nest-cli.json                     # Конфигурация NestJS
│   └── tsconfig.json                     # Конфигурация TypeScript
│
└── frontend/                             # Frontend приложение (Angular)
    ├── src/
    │   ├── app/                          # Код приложения
    │   │   ├── core/                     # Ядро (auth)
    │   │   ├── features/                 # Функциональные модули
    │   │   ├── shared/                   # Переиспользуемое
    │   │   ├── views/                    # Страницы
    │   │   ├── app.config.ts             # Конфигурация приложения
    │   │   └── app.routes.ts             # Маршруты
    │   ├── assets/                       # Статические ресурсы
    │   │   ├── fonts/                    # Шрифты
    │   │   ├── images/                   # Изображения
    │   │   ├── styles/                   # Глобальные стили
    │   │   └── theme/                    # Тема PrimeNG
    │   ├── environments/                 # Конфигурация окружений
    │   ├── index.html                    # HTML шаблон
    │   ├── main.ts                       # Точка входа
    │   └── styles.scss                   # Глобальные стили
    ├── package.json                      # Зависимости
    ├── angular.json                      # Конфигурация Angular
    └── tsconfig.json                     # Конфигурация TypeScript
```

---

## 🚀 Установка и запуск

### Требования

- **Node.js**: >= 18.x
- **npm**: >= 10.x
- **MongoDB**: >= 6.x (локально или в облаке)

### Backend

1. **Перейти в директорию backend:**
```bash
cd backend
```

2. **Установить зависимости:**
```bash
npm install
```

3. **Создать файл .env:**
```env
# MongoDB
MONGODB_URI=mongodb://localhost:27017/online-shop

# JWT
JWT_SECRET=your-secret-key-change-this
JWT_EXPIRES_IN=15m
JWT_REFRESH_SECRET=your-refresh-secret-key-change-this
JWT_REFRESH_EXPIRES_IN=7d

# App
PORT=3000
```

4. **Запустить в режиме разработки:**
```bash
npm run start:dev
```

Backend будет доступен по адресу: `http://localhost:3000`

5. **Команды для разработки:**
```bash
npm run start          # Запуск в production режиме
npm run start:dev      # Запуск в dev режиме (hot-reload)
npm run build          # Сборка проекта
npm run test           # Запуск тестов
npm run lint           # Проверка кода
npm run format         # Форматирование кода
```

### Frontend

1. **Перейти в директорию frontend:**
```bash
cd frontend
```

2. **Установить зависимости:**
```bash
npm install
```

3. **Настроить окружение (если необходимо):**
Файл `src/environments/environment.ts`:
```typescript
export const environment = {
  production: false,
  apiUrl: 'http://localhost:3000'
};
```

4. **Запустить в режиме разработки:**
```bash
npm start
```

Frontend будет доступен по адресу: `http://localhost:4200`

5. **Команды для разработки:**
```bash
npm start              # Запуск dev сервера
npm run build          # Сборка для production
npm run watch          # Сборка в watch режиме
npm run test           # Запуск тестов
npm run lint           # Проверка кода
npm run lint:fix       # Автоисправление
npm run format         # Форматирование кода
```

---

## 📝 Ключевые особенности архитектуры

### Backend

1. **Модульность**: Каждая функция выделена в отдельный модуль с четкой ответственностью
2. **Dependency Injection**: Все зависимости инжектируются через конструктор
3. **DTOs**: Использование Data Transfer Objects для валидации входных данных
4. **Guards**: Защита эндпоинтов на уровне маршрутов
5. **Decorators**: Кастомные декораторы для упрощения кода
6. **Middleware/Interceptors**: Обработка запросов на глобальном уровне
7. **MongoDB Schemas**: Строгая типизация данных в базе

### Frontend

1. **Standalone Components**: Использование новых standalone компонентов Angular
2. **Signals**: Реактивное управление состоянием через @ngrx/signals
3. **Lazy Loading**: Ленивая загрузка модулей для оптимизации
4. **Guards**: Защита маршрутов на клиенте
5. **Interceptors**: Автоматическое добавление токенов к запросам
6. **Pipes**: Преобразование данных в шаблонах
7. **Services**: Бизнес-логика вынесена в сервисы
8. **Responsive Design**: Адаптивный дизайн с Tailwind CSS

---

## 🔒 Безопасность

### Backend
- Хеширование паролей с bcrypt (salt rounds: 10)
- JWT токены с ограниченным временем жизни
- Refresh токены для обновления доступа
- Валидация всех входных данных
- Role-based access control (RBAC)
- CORS настройки

### Frontend
- Хранение токенов в localStorage (можно улучшить до httpOnly cookies)
- Автоматическое обновление токенов
- Защита маршрутов через guards
- Валидация форм на клиенте
- Sanitization пользовательского ввода

---

## 📚 Документация API

Для тестирования API доступна коллекция Postman:
- Файл: `backend/Online-Shop-Backend.postman_collection.json`

Импортируйте эту коллекцию в Postman для быстрого тестирования всех эндпоинтов.

---

## 🎯 Дальнейшее развитие

### Планируемые улучшения:

**Backend:**
- [ ] Swagger документация (OpenAPI)
- [ ] WebSockets для real-time уведомлений
- [ ] Email сервис для подтверждения регистрации
- [ ] Система платежей (Stripe/PayPal)
- [ ] Логирование (Winston)
- [ ] Rate limiting для защиты от DDoS
- [ ] Redis для кэширования

**Frontend:**
- [ ] Progressive Web App (PWA)
- [ ] Server-Side Rendering (SSR/SSG)
- [ ] Internationalization (i18n)
- [ ] Dark mode
- [ ] Wishlist функционал
- [ ] Сравнение товаров
- [ ] Продвинутые фильтры
- [ ] Отзывы с изображениями

---

## 👥 Роли пользователей

### User (Пользователь)
- Просмотр каталога товаров
- Добавление товаров в корзину
- Оформление заказов
- Написание комментариев
- Управление профилем

### Admin (Администратор)
- Все возможности пользователя +
- Создание/редактирование/удаление товаров
- Управление категориями
- Модерация комментариев
- Просмотр статистики

---

## 📞 Контакты и поддержка

Для вопросов и предложений по проекту создавайте Issues в репозитории.

---

**Версия документации:** 1.0.0  
**Дата обновления:** Январь 2026
