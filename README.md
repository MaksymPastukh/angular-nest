# 🛍️ Online Marketplace - Интернет Магазин

Полнофункциональное приложение онлайн-магазина с корзиной, системой авторизации, каталогом товаров и административной панелью.

## 📋 Оглавление

- [О проекте](#о-проекте)
- [Технологический стек](#технологический-стек)
- [Архитектура проекта](#архитектура-проекта)
- [Быстрый старт](#быстрый-старт)
- [Структура проекта](#структура-проекта)
- [Основной функционал](#основной-функционал)
- [API Endpoints](#api-endpoints)
- [Разработка](#разработка)
- [Скрипты](#скрипты)

---

## 🎯 О проекте

Современное веб-приложение интернет-магазина с микросервисной архитектурой, построенное на Angular и NestJS. Проект демонстрирует лучшие практики разработки:

- ✅ Модульная архитектура (Feature-based)
- ✅ Reactive состояние (@ngrx/signals)
- ✅ JWT авторизация с refresh токенами
- ✅ RESTful API с валидацией
- ✅ Адаптивный UI (PrimeNG)
- ✅ TypeScript строгая типизация
- ✅ MongoDB для хранения данных

---

## 🛠 Технологический стек

### Frontend

- **Framework**: Angular 21.0
- **State Management**: @ngrx/signals
- **UI Library**: PrimeNG 21.0, PrimeIcons
- **Styling**: SCSS
- **HTTP Client**: Angular HttpClient
- **Forms**: Signal Forms
- **Router**: Angular Router with Guards

### Backend

- **Framework**: NestJS 11.0
- **Database**: MongoDB (Mongoose 9.0)
- **Authentication**: Passport.js (JWT, Local)
- **Validation**: class-validator, class-transformer
- **Password Hashing**: bcrypt
- **File Upload**: @nestjs/platform-express

### DevOps

- **Package Manager**: npm
- **Linting**: ESLint
- **Formatting**: Prettier
- **Testing**: Jest, Vitest

---

## 🏗️ Архитектура проекта

```
Marketplace/
│
├── backend/          # NestJS API сервер
│   ├── src/
│   │   ├── auth/     # Модуль авторизации (JWT, Guards)
│   │   ├── cart/     # Модуль корзины покупок
│   │   ├── comments/ # Модуль комментариев/отзывов
│   │   ├── products/ # Модуль товаров (CRUD)
│   │   ├── users/    # Модуль пользователей
│   │   └── static/   # Модуль статических файлов
│   └── public/
│       └── images/   # Загруженные изображения товаров
│
└── frontend/         # Angular приложение
    └── src/
        └── app/
            ├── core/          # Ядро (Guards, Interceptors, Layout)
            ├── features/      # Фичи/модули приложения
            │   ├── auth/      # Авторизация/регистрация
            │   ├── admin/     # Админ панель
            │   ├── catalog/   # Каталог товаров
            │   ├── cart/      # Корзина
            │   ├── products/  # Страницы товаров
            │   └── categories/# Категории
            └── shared/        # Переиспользуемые компоненты
```

---

## 🚀 Быстрый старт

### Предварительные требования

- Node.js >= 18.x
- npm >= 10.x
- MongoDB >= 6.x (запущенный локально или в облаке)

### Установка

1. **Клонировать репозиторий**

```bash
git clone <repository-url>
cd Marketplace
```

2. **Установить зависимости Backend**

```bash
cd backend
npm install
```

3. **Настроить переменные окружения Backend**

Создайте файл `.env` в папке `backend/`:

```env
# Database
MONGODB_URI=mongodb://localhost:27017/marketplace

# JWT
JWT_SECRET=your-super-secret-jwt-key
JWT_EXPIRATION=15m
REFRESH_TOKEN_SECRET=your-refresh-token-secret
REFRESH_TOKEN_EXPIRATION=7d

# Server
PORT=3000
```

4. **Запустить Backend**

```bash
npm run start:dev
```

Backend будет доступен на `http://localhost:3000`

5. **Установить зависимости Frontend**

```bash
cd ../frontend
npm install
```

6. **Настроить окружение Frontend**

Проверьте файл `src/environments/environment.development.ts`:

```typescript
export const environment = {
  production: false,
  apiUrl: "http://localhost:3000",
};
```

7. **Запустить Frontend**

```bash
npm start
```

Приложение откроется на `http://localhost:4200`

---

## 📁 Структура проекта

### Backend (NestJS)

```
backend/src/
├── auth/                      # Модуль авторизации
│   ├── guards/                # JWT, Local, Roles Guards
│   ├── strategies/            # Passport стратегии
│   ├── decorators/            # Custom декораторы (@CurrentUser, @Roles)
│   └── dto/                   # DTO для login/register/refresh
│
├── cart/                      # Модуль корзины
│   ├── schemas/               # MongoDB схемы корзины
│   ├── dto/                   # DTO для операций с корзиной
│   └── types/                 # TypeScript интерфейсы
│
├── products/                  # Модуль товаров
│   ├── schemas/               # MongoDB схемы товаров
│   ├── dto/                   # DTO для CRUD операций
│   └── interfaces/            # Интерфейсы товаров
│
├── comments/                  # Модуль комментариев
│   ├── schemas/               # Схема комментариев
│   └── dto/                   # DTO для комментариев
│
└── users/                     # Модуль пользователей
    └── schemas/               # Схема пользователя
```

### Frontend (Angular)

```
frontend/src/app/
├── core/                      # Ядро приложения
│   ├── guards/                # Route Guards (auth, admin, auth-forward)
│   ├── http/                  # HTTP Interceptors (auth.interceptor)
│   └── layout/                # Layout компоненты (header, footer)
│
├── features/                  # Feature модули
│   ├── auth/                  # Авторизация
│   │   ├── data-access/       # Сервисы (auth.service, token-refresh.service)
│   │   ├── store/             # @ngrx/signals Store
│   │   ├── pages/             # Страницы (login, register)
│   │   └── ui/                # UI компоненты
│   │
│   ├── admin/                 # Админ панель
│   │   ├── data-access/       # Admin сервисы
│   │   ├── store/             # Admin Store
│   │   └── pages/             # Страницы управления
│   │
│   ├── catalog/               # Каталог товаров
│   │   ├── data-access/       # Catalog сервисы
│   │   ├── store/             # Catalog Store
│   │   └── pages/             # Страницы каталога
│   │
│   ├── products/              # Товары
│   │   ├── data-access/       # Products сервисы
│   │   └── pages/             # Страницы товара (детали)
│   │
│   └── cart/                  # Корзина (планируется)
│
└── shared/                    # Общие компоненты
    ├── pipes/                 # Pipes (image-url.pipe)
    └── ui/                    # Переиспользуемые UI компоненты
```

---

## ✨ Основной функционал

### 🔐 Авторизация и аутентификация

- ✅ Регистрация пользователей с хэшированием паролей (bcrypt)
- ✅ Вход по email/пароль
- ✅ JWT токены (Access + Refresh)
- ✅ Автоматическое обновление токенов
- ✅ Route Guards для защиты маршрутов
- ✅ Ролевая система (user, admin)

### 🛒 Корзина покупок

- ✅ Добавление товаров с выбором размера/цвета
- ✅ Изменение количества товаров
- ✅ Удаление товаров
- ✅ Автоматический пересчет итоговой стоимости
- ✅ Очистка корзины
- ✅ Сохранение корзины в базе данных

### 📦 Управление товарами

- ✅ CRUD операции для товаров
- ✅ Загрузка изображений товаров
- ✅ Фильтрация и поиск
- ✅ Пагинация
- ✅ Категории товаров
- ✅ Рейтинг и отзывы

### 👨‍💼 Административная панель

- ✅ Создание/редактирование товаров
- ✅ Управление категориями
- ✅ Управление пользователями (планируется)
- ✅ Статистика продаж (планируется)

### 💬 Комментарии и отзывы

- ✅ Добавление комментариев к товарам
- ✅ Редактирование/удаление собственных комментариев
- ✅ Рейтинг товаров

---

## 🔌 API Endpoints

### Authentication

```
POST   /api/auth/register          # Регистрация
POST   /api/auth/login             # Вход
POST   /api/auth/refresh           # Обновление токена
GET    /api/auth/profile           # Получить профиль (Protected)
```

### Products

```
GET    /api/products               # Список товаров (фильтры, пагинация)
GET    /api/products/:id           # Детали товара
POST   /api/products               # Создать товар (Admin)
PATCH  /api/products/:id           # Обновить товар (Admin)
DELETE /api/products/:id           # Удалить товар (Admin)
POST   /api/products/:id/image     # Загрузить изображение (Admin)
```

### Cart

```
GET    /api/cart                   # Получить корзину (Protected)
POST   /api/cart/add               # Добавить в корзину (Protected)
PATCH  /api/cart/update            # Обновить количество (Protected)
DELETE /api/cart/remove/:productId # Удалить из корзины (Protected)
DELETE /api/cart/clear             # Очистить корзину (Protected)
```

### Comments

```
GET    /api/comments               # Список комментариев (фильтры)
POST   /api/comments               # Создать комментарий (Protected)
PATCH  /api/comments/:id           # Обновить комментарий (Protected)
DELETE /api/comments/:id           # Удалить комментарий (Protected)
```

### Static Files

```
GET    /api/static/images/:filename # Получить изображение
```

Подробная документация: [CART_API.md](backend/CART_API.md)

---

## 💻 Разработка

### Backend разработка

```bash
cd backend

# Запуск в режиме разработки (hot-reload)
npm run start:dev

# Запуск в debug режиме
npm run start:debug

# Сборка для production
npm run build

# Запуск production сборки
npm run start:prod

# Линтинг
npm run lint

# Форматирование
npm run format

# Тестирование
npm run test              # Unit тесты
npm run test:watch        # Watch режим
npm run test:e2e          # E2E тесты
```

### Frontend разработка

```bash
cd frontend

# Запуск dev сервера
npm start                 # или ng serve

# Сборка для production
npm run build

# Watch режим для сборки
npm run watch

# Линтинг
npm run lint
npm run lint:fix

# Форматирование
npm run format
npm run format:check

# Линтинг + форматирование
npm run lint:format

# Тестирование
npm test
```

---

## 📝 Скрипты

### Backend Scripts

| Команда               | Описание                                   |
| --------------------- | ------------------------------------------ |
| `npm run start`       | Запустить приложение                       |
| `npm run start:dev`   | Запустить в режиме разработки с hot-reload |
| `npm run start:debug` | Запустить с отладкой                       |
| `npm run start:prod`  | Запустить production сборку                |
| `npm run build`       | Собрать приложение                         |
| `npm run lint`        | Проверить код ESLint                       |
| `npm run format`      | Форматировать код Prettier                 |
| `npm test`            | Запустить unit тесты                       |
| `npm run test:e2e`    | Запустить e2e тесты                        |

### Frontend Scripts

| Команда                | Описание                              |
| ---------------------- | ------------------------------------- |
| `npm start`            | Запустить dev сервер (localhost:4200) |
| `npm run build`        | Собрать приложение для production     |
| `npm run watch`        | Собрать в watch режиме                |
| `npm run lint`         | Проверить код ESLint                  |
| `npm run lint:fix`     | Исправить проблемы ESLint             |
| `npm run format`       | Форматировать код Prettier            |
| `npm run format:check` | Проверить форматирование              |
| `npm run lint:format`  | Линтинг + форматирование              |
| `npm test`             | Запустить тесты Vitest                |

---

## 🎨 UI/UX

- **UI Framework**: PrimeNG (современные компоненты)
- **Icons**: PrimeIcons
- **Тема**: Aura Light (настраиваемая)
- **Адаптивность**: Mobile-first подход
- **Шрифты**: Пользовательские шрифты в `assets/fonts/`

---

## 🗂️ Основные концепции

### Feature-Based архитектура (Frontend)

Каждая фича содержит:

- **data-access/** - Сервисы для API вызовов
- **domain/** - Интерфейсы, типы, константы, enums
- **store/** - State management (@ngrx/signals)
- **pages/** - Smart компоненты-страницы
- **ui/** - Presentational компоненты
- **utils/** - Вспомогательные функции
- **routes.ts** - Маршруты модуля

### Module-Based архитектура (Backend)

Каждый модуль содержит:

- **module.ts** - Определение модуля
- **controller.ts** - HTTP endpoints
- **service.ts** - Бизнес-логика
- **schemas/** - MongoDB схемы (Mongoose)
- **dto/** - Data Transfer Objects (валидация)
- **interfaces/** - TypeScript интерфейсы
- **guards/** - Route guards
- **decorators/** - Custom декораторы

---

## 🔒 Безопасность

- ✅ Хэширование паролей (bcrypt)
- ✅ JWT токены (short-lived access + long-lived refresh)
- ✅ HTTP-only cookies для токенов (опционально)
- ✅ CORS настройка
- ✅ Валидация всех входных данных (class-validator)
- ✅ Guards для защиты маршрутов
- ✅ Role-based access control (RBAC)

---

## 📚 Дополнительная документация

- [Backend Cart API](backend/CART_API.md) - Детальная документация по API корзины
- [Cart Angular Integration](backend/CART_ANGULAR_INTEGRATION.md) - Интеграция корзины с Angular
- [Postman Collection](backend/Online-Shop-Backend.postman_collection.json) - Коллекция для тестирования API

---

## 🚧 В разработке

- [ ] Модуль заказов (Orders)
- [ ] Интеграция платежных систем
- [ ] Личный кабинет пользователя
- [ ] История заказов
- [ ] Email уведомления
- [ ] Поиск с автодополнением
- [ ] Фильтры по категориям
- [ ] Сравнение товаров
- [ ] Список желаемого (Wishlist)
- [ ] Отслеживание заказов

---

## 📄 Лицензия

UNLICENSED - Частный проект

---

## 👤 Автор

Разработано как учебный проект для демонстрации Full-Stack разработки с Angular и NestJS.

---

## 🤝 Вклад

Проект находится в активной разработке. Предложения и улучшения приветствуются!

---

**Стек технологий**: Angular • NestJS • MongoDB • TypeScript • RxJS • JWT • PrimeNG
